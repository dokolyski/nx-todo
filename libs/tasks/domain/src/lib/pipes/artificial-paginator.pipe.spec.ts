import { ArtificialPaginatorPipe } from './artificial-paginator.pipe';

describe('ArtificialPaginatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtificialPaginatorPipe();
    expect(pipe).toBeTruthy();
  });
});
